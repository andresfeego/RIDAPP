import React, { Component } from 'react';
import { View, Text } from 'react-native';

class GeneralMenu extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> GeneralMenu </Text>
      </View>
    );
  }
}

export default GeneralMenu;
